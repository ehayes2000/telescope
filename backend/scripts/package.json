{
	"name": "scripts",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"scripts": {
		"download": "bun run src/init.ts && bun run src/download.ts > download_log.txt",
		"index": "bun run src/build_index.ts",
		"metadata": "bun run src/build_metadata.ts"
	},
	"dependencies": {
		"@aws-sdk/client-s3": "^3.901.0",
		"fast-xml-parser": "^5.3.0"
	}
}
